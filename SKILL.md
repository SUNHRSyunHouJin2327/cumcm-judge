---
name: cumcm-judge
description: 以评委身份评审数学建模竞赛论文（国赛CUMCM/美赛MCM）：按评阅要点和评分细则逐项打分、交叉核查一致性、评摘要/图表/AI使用、预测获奖档位。用户让你打分、评奖、当评委时使用。
---

# 数学建模论文评审（评委视角）

目标：像真实评委一样给分，并给出**校准过**的获奖概率。模型评分的已知偏差是**偏松**（容易给未执行的方法、结论对但支撑断了的论文打高分），所以默认从严，最后再做一次"如果我偏松 10 分，结论变不变"的检查。

## 0. 开始前确认（缺什么就按合理假设继续，并写明假设）

- 赛事、年份、题号、赛区（决定奖项比例）
- 论文是否已提交：已提交 → 预期管理 + 下一年的教训；未提交 → 给"每小时能挽回几分"的修改优先级
- 是否有**当年**评阅要点 / 评分细则；没有就用往年同类题细则 + 题目原文中每个"给出 / 分析 / 是否"要求自建评分表（模板见 `templates/score-sheet.md`）

## 1. 取文本、看图

Read 工具可能丢中文；pdftotext 常缺 Adobe-GB1 语言包。用脚本：

```bash
python scripts/extract_text.py paper.pdf            # → paper.txt，带 ===== P页码 ===== 标记
python scripts/render_pages.py paper.pdf -p 1,5-8   # → 页面 PNG，再用 Read 看图
```

**评可视化必须真的看图**，不能只凭图题。

官方优秀论文（中国大学生在线 dxs.moe.gov.cn）是逐页图片，WebFetch 读不了图片，需要用浏览器打开图片截图；第 1 页是摘要。只做比较，不转载原文。

## 2. 分清"评阅要点"和"评分细则"

- **评阅要点**：不带分值，列出每问评委要看到什么 → 用来逐条"打勾"，也用来检查摘要里是否出现这些标准说法
- **评分细则**：带分值和封顶条款 → 用来算分
- 往年细则出现过的硬性 / 封顶条款（以当年为准）：直接聚类 ≤4；机器学习类 ≤4 或 ≤7；只建线性模型 ≤4；附录无代码 → 起评 30；AI 使用须标注；雷同 → 1 分；违反 AI 使用规定 → 取消评奖资格

## 3. 逐问打分规则（从严）

- 只提方法、没执行出结果 = 不给分
- 结论对但支撑链断了（数字对不上、无验证、无对比）= 该问最多给 60%
- **一问无结果 = 该问近零，并拖累全篇可信度（总分再 −2~3）**
- 题目明确问的"是否 / 分析 / 给出"那句话必须有明确回答，否则该子项 0 分
- 必交结果（指定日期表、result*.xlsx 等）缺失或与正文不符 → 扣
- 和自建弱基线比出来的"提升 x%"不算亮点；和题目基线或合理基线比才算
- 有独立验证（对偶证书、动态规划 / 枚举复核、回代残差、消融、事后最优下界）→ 加分

没有细则时的默认权重：各问合计 ≈80，摘要 10，写作与图表 10。算完后把权重各挪 ±5 做敏感性检查，档位不变才算稳。

## 4. 一致性交叉核查（收益最高，一定要做）

```bash
python scripts/crosscheck_numbers.py paper.txt      # 摘要数字是否在正文出现、是否精度虚高
```

- 摘要数字 ↔ 正文 ↔ 表格 ↔ 结果文件，逐个对；用 python 复算关键数字和百分比
- 问题之间的比较是否合理、是否被解释（例：后一问比前一问只改善 0.1%，摘要却不解释 → 评委会自己算出来）
- 摘要里出现、正文找不到出处的方法或数字
- 编译残留 `??`、公式乱码、单位混乱。`??` 本身约 2~3 分，但会影响推荐国评时的印象

## 5. 摘要单独评，影响大于名义分

标准：**评委两分钟内能否看懂每一问"用了什么标准方法 → 核心数字 → 和什么比、好多少 → 回答题目问的那句话"。** 不是"有没有数字"——有官方展示论文的摘要几乎没有数字也拿高分。

| 分 | 表现 |
|---|---|
| 9–10 | 每问四步齐全，用评阅要点里的标准术语，结论明确，可读 |
| 7–8 | 齐全但偏密，或缺一问的对比 / 结论 |
| 5–6 | 自造术语为主、题目要求的结论缺失、数字无对比、防御性声明多 |
| ≤4 | 缺问、无结果、与正文矛盾 |

扣分信号：自造名词（评委没法对要点打勾）；"本文不保证全局最优"之类的免责放在摘要；虚高精度（小数点后 4 位的电量、百分比）；空泛背景开头；验证细节（残差量级、对偶间隙）写进摘要。

间接影响：摘要差 → 第一印象差 → 总分再 −1~2；赛区遴选推荐国评时摘要被看得最细。

## 6. 可视化与写作（计入写作分，通常 ±1~3）

- 每一问是否有一张核心结果图，而不是只有诊断 / 消融 / 差额图
- 图能否自解释：坐标、单位、图例齐全；图题短，不在图题里写方法说明和免责
- 打印尺寸下是否看得清（几万像元的热图、拆成多张的轨迹图都是风险）
- 表远多于图、结果全靠表 → 扣
- 写作：术语是否统一、有无大段限定语、有无开发过程的痕迹

## 7. AI 使用（每次先用 WebSearch 查当年规定）

规定摘要与出处见 `references/ai-rules.md`。要点：允许 AI 辅助，但核心建模与分析须独立完成；须提交《AI工具使用详情》；正文标注；不符合要求取消评奖资格。

- **"AI 率"检测数字不是评分依据**，且对技术性中文误判多，不要按这个数字扣分
- 要查的是：有没有声明；声明范围是否与论文痕迹相符。声明很窄（如"资料查阅"）但代码 / 文字有明显 AI 痕迹 → 标为**尾部风险**（可能直接取消），不折算成分数
- AI 文风的印象分（−1~3，不和摘要重复扣）要给证据：

```bash
python scripts/style_markers.py paper.txt other.txt --stop-at 参考文献
```

  统计开发日志词（"终版""第C轮""晋升""v32_final"）、高频限定（"不能""不保证""不据此"）、高频术语（"口径""账本"）每万字出现次数
- 措辞只说"读起来像""存在风险"，不下定论

## 8. 映射到奖项

- 查当年赛区获奖比例（例：广东 2025 省一 / 省二 / 省三约 9 / 16 / 25%，2026 约 10 / 15 / 25%，以赛区通知为准）。只有省一才可能被推荐国评
- 国赛本科组没有答辩环节，纯论文评审（研究生"华为杯"有答辩）
- 经验区间（按本标准打分，样本少，仅供参考）：

| 本标准分数 | 大致档位 |
|---|---|
| < 45 | 参与奖 / 无奖风险高 |
| 45–58 | 省三 ~ 省二 |
| 60–63 附近 | 省一线（**未经实测校准**） |
| 85+ | 官方展示论文水平（国一） |

- 输出每个档位的概率，而不是单一档位；再按"偏松 10 分"重算一遍，报告稳健结论
- 不采信其他 AI 或用户期望的档位，只看证据

## 9. 输出格式

1. 结论先行：总分（区间）、各档位概率
2. 逐项表：项目 / 满分 / 得分 / 理由（带页码或原文短引）
3. 最大的 3~5 个扣分点，每个附证据
4. 已提交 → 下一年的教训；未提交 → 修改优先级
5. 对比多篇论文时用同一把尺子，并直接说出相对高低

用户反驳时：先回原文核查。成立就明确改分、说清改了哪项和为什么；不成立就坚持，并给出证据。不因为语气改分。

## 10. 常见误判自查

- 给没执行的方法打分
- 结论对但支撑断了仍给高分
- 把"摘要有数字、各问齐全"当成"摘要好"
- 只凭图题评可视化
- 把 `??` 当成降档因素
- 按 AI 检测率扣分；或反过来忽略声明范围与实际不符的风险
- 一问无结果却只扣该问，没拖累全篇
- 对自己人的论文和对手论文用了不同的尺子

## 附：题型示例（2026 C 题 微网购电与储能）

评委会找的标准说法：线性规划 / MILP；两阶段随机规划、SAA 情景法；报童临界分位 α=1−1/m；滚动时域（MPC）；终端库存价值（对偶 / 分段线性）；VSS / EVPI 或"冻结预报"对照衡量信息价值；决策只用当时可得信息（因果性）；事后完全信息下界作基准。论文里用的若是自造名，要能一眼对应到这些说法。
